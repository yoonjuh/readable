import React from 'react';
import styled from 'styled-components';

const StyledSidebar = styled.aside`
  width: 12rem;
`;

const Sidebar = () => <StyledSidebar>Hey! It's a sidebar!</StyledSidebar>;

export default Sidebar;
